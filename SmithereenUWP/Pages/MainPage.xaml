<Page
    x:Class="SmithereenUWP.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmithereenUWP.Pages"
    xmlns:ctrls="using:SmithereenUWP.Controls"
    xmlns:vm="using:SmithereenUWP.ViewModels"
    xmlns:models="using:SmithereenUWP.DataModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loading="Page_Loading" Loaded="Page_Loaded">

    <Page.Resources>
        <DataTemplate x:Key="MenuItemTemplate" x:DataType="models:MainMenuItem">
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <ctrls:FixedFontIcon FontSize="16" Glyph="{x:Bind Icon}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{x:Bind Label}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="MenuTemplate" x:DataType="vm:SessionViewModel">
            <ListView ItemsSource="{x:Bind MenuItems, Mode=OneWay}" SelectedItem="{x:Bind SelectedMenuItem, Mode=OneWay}" ItemTemplate="{StaticResource MenuItemTemplate}"
                      SelectionMode="Single" SingleSelectionFollowsFocus="False" IsItemClickEnabled="True" ItemClick="MainMenu_ItemClick"/>
        </DataTemplate>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Rectangle x:Name="L" Grid.Column="0" Grid.RowSpan="5"/>
        <Rectangle x:Name="R" Grid.Column="2" Grid.RowSpan="5"/>
        <Rectangle x:Name="T" Grid.Row="0" Grid.ColumnSpan="3"/>
        <Rectangle x:Name="B" Grid.Row="4" Grid.ColumnSpan="3"/>

        <Frame x:Name="PageContent" Grid.Column="2" Grid.Row="0" Grid.ColumnSpan="1" Grid.RowSpan="4" Navigated="PageContent_Navigated"/>

        <Border x:Name="PageHeaderBackground" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Grid.RowSpan="2"/>
        <Grid x:Name="PageHeader" Grid.Row="1" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="MenuButton" Style="{StaticResource ControlButtonStyle}" Content="" Click="OpenNarrowMenu"/>

            <ContentPresenter x:Name="PageHeaderContent" Grid.Column="1" Padding="16,0" VerticalAlignment="Stretch" VerticalContentAlignment="Center" FontWeight="SemiBold"/>
            <ContentPresenter x:Name="HeaderAfterContent" Grid.Column="2" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch"/>
        </Grid>

        <Grid x:Name="NarrowMenuLayer" Visibility="Collapsed" Grid.ColumnSpan="4" Grid.RowSpan="5" Background="Transparent" Tapped="CloseNarrowMenu"/>
        <Grid x:Name="NarrowMenuContainer" Visibility="Collapsed" Grid.ColumnSpan="2" Grid.RowSpan="5" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"/>
        <ContentControl x:Name="NarrowMenu" Visibility="Collapsed" Grid.Column="1" Grid.Row="1" Grid.RowSpan="3" Width="240" HorizontalContentAlignment="Stretch" ContentTemplate="{StaticResource MenuTemplate}"/>

        <Grid x:Name="WideMenuContainer" Grid.ColumnSpan="2" Grid.RowSpan="5" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"/>
        <ContentControl x:Name="WideMenu" Grid.Column="1" Grid.Row="1" Grid.RowSpan="3" Width="320" HorizontalContentAlignment="Stretch" ContentTemplate="{StaticResource MenuTemplate}"/>
    </Grid>
</Page>

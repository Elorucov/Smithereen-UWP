<UserControl
    x:Class="SmithereenUWP.Controls.HyperlinkActionButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmithereenUWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <HyperlinkButton x:Name="MainButton" Background="{Binding Background}" Style="{StaticResource WallPostHyperlinkStyle}">
        <StackPanel x:Name="HABRoot" Orientation="Horizontal" Margin="-3,0">
            <TextBlock x:Name="HABLabel" Margin="3,0,3,-2" Text="{Binding Label}">
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="-2"/>
                </TextBlock.RenderTransform>
            </TextBlock>
            <local:FixedFontIcon x:Name="HABIcon" Margin="3,0" Glyph="{Binding IconGlyph}"/>
            <TextBlock x:Name="HABCounter" Margin="3,0,3,0" FontWeight="SemiBold" Text="{Binding Count}">
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="-2"/>
                </TextBlock.RenderTransform>
            </TextBlock>
        </StackPanel>
    </HyperlinkButton>
</UserControl>
